# Usage

```cpp
#include <iostream>
#include "./src/tree.hh"
using namespace std;

int main()
{
    // Creates a list where the nodes are strings
    list<string> l;

    // Add values to l
    l.insert("oradea");
    l.insert("sibiu");
    l.insert("zerind");

    return 0;
}
```
